{
  "name": "renpy-to-ps3",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sidecar": "node scripts/build-sidecar.mjs",
    "predev": "npm run sidecar",
    "dev": "vite",
    "prebuild": "npm run sidecar",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "tauri": "tauri"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "@tauri-apps/plugin-dialog": "^2",
    "@tauri-apps/plugin-shell": "^2"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "typescript": "~6.0.3",
    "vite": "^8.0.16"
  }
}
